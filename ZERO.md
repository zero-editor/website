<!-- ZERO.md — zero's setup file for this project, one per repository,
     the way a CLAUDE.md is. It holds one thing so far: the vocabulary
     this project's voice memos are transcribed and cleaned up against.
     One term per line: "- Term — notes about how transcription mangles it".
     The term is fed to speech recognition; the whole line guides cleanup.
     Meant to be committed, so the project's words travel with the repo. -->
- website — this project's name; restore it exactly as written here if the transcription mangles it.
- zero — a project name that is an ordinary word; will be transcribed as the numeral or lost entirely
- Astro — heard as "astro" lowercase or "Astra"; the build framework's name
- Railway — heard as the ordinary word "railway"; here it's the deploy platform
- Caddy — heard as "caddie" or "Katie"; the web server
- Caddyfile — heard as "caddy file" or "caddie file"; one word, capital C and F
- dmg — spelled out or heard as "DMG"/"dee em gee"; the macOS disk image extension
- macOS — heard as "Mac OS" or "MacOS"; lowercase m, capital OS
- SF Mono — heard as "SF mono", "es eff mono", or "surf mono"; the typeface
- npm — heard as "NPM" or "en pee em"; always lowercase
- zero-editor — the GitHub org; heard as "zero editor" without the hyphen
- index.astro — heard as "index astro" or "index dot astro"; the page file
- astro.config.mjs — heard as "astro config M J S"; the .mjs extension is easily mangled
- railway.json — heard as "railway JSON" or "railway dot json"
- Dockerfile — heard as "docker file"; one word, capital D
- og:image — heard as "OG image" or "oh gee image"; the meta tag
- App.css — heard as "app CSS" or "app dot CSS"
- GitHub — heard as "git hub" or "Github"; capital H
- bench/ — the benchmark directory; heard as the ordinary word "bench"
- dist/ — heard as "dist", "disk", or "dissed"; the build output directory
- localhost:4321 — the port digits are easily garbled
- Cursor — misheard as: cursor (suggested)
