---
// The whole site is this file. That’s not laziness, it’s a position.
const REPO = 'zero-editor/zero';
const GITHUB = `https://github.com/${REPO}`;
const DMG = `${GITHUB}/releases/latest/download/zero_aarch64.dmg`;
const BENCHMARKS = `${GITHUB}/blob/main/BENCHMARKS.md`;

// Minimal stroke icons, 24-grid. Rendered inline via set:html.
const IC: Record<string, string> = {
  box: '<path d="M12 3 4 7v10l8 4 8-4V7l-8-4Z"/><path d="m4 7 8 4 8-4"/><path d="M12 11v10"/>',
  watch: '<circle cx="12" cy="13" r="8"/><path d="M12 9v4l2.5 2.5"/><path d="M9.5 2.5h5"/>',
  chip: '<rect x="7" y="7" width="10" height="10" rx="1"/><path d="M4 10h3M4 14h3M17 10h3M17 14h3M10 4v3M14 4v3M10 17v3M14 17v3"/>',
  files: '<path d="M9 3h7l4 4v12H9Z"/><path d="M15 3v4h5"/><path d="M5 8v13h11"/>',
  braces: '<path d="M8.5 4C6.5 4 7 6 7 7.5S7 11 5 12c2 1 2 2.5 2 4.5S6.5 20 8.5 20"/><path d="M15.5 4c2 0 1.5 2 1.5 3.5s0 3.5 2 4.5c-2 1-2 2.5-2 4.5s.5 3.5-1.5 3.5"/>',
  compass: '<circle cx="12" cy="12" r="9"/><path d="m9.5 14.5 1.5-4 4-1.5-1.5 4z"/>',
  grid: '<rect x="4" y="4" width="7" height="7" rx="1"/><rect x="13" y="4" width="7" height="7" rx="1"/><rect x="4" y="13" width="7" height="7" rx="1"/><rect x="13" y="13" width="7" height="7" rx="1"/>',
  gauge: '<path d="M5 19a9 9 0 1 1 14 0"/><path d="m12 14 4.5-4.5"/>',
  tabs: '<rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18M9 5v5M15 5v5"/>',
  term: '<rect x="3" y="4" width="18" height="16" rx="2"/><path d="m7 9 3 3-3 3"/><path d="M12 15h5"/>',
  branch: '<circle cx="6" cy="6" r="2"/><circle cx="6" cy="18" r="2"/><circle cx="18" cy="7" r="2"/><path d="M6 8v8"/><path d="M18 9c0 5-9 3.5-11 7"/>',
  bars: '<path d="M6 5v14"/><path d="M12 9v10"/><path d="M18 4v9"/>',
  search: '<circle cx="10.5" cy="10.5" r="6.5"/><path d="M15.5 15.5 21 21"/>',
  bolt: '<path d="M13 2 5 13.5h5L9 22l8-11.5h-5z"/>',
  cursor: '<path d="m5 4 13 6.5-5.5 1.5 3.5 5.5-2.5 1.5-3.5-5.5L6 17z"/>',
  restore: '<path d="M4 5v6h6"/><path d="M4.5 11a8 8 0 1 0 2.5-6"/>',
  slash: '<circle cx="12" cy="12" r="9"/><path d="m6 6 12 12"/>',
};

// Receipts: zero vs cursor, bar widths from the raw ratio. Same machine,
// same project, three launches each, median. r === 0 draws no bar at all.
const RECEIPTS = [
  { icon: 'box',     label: 'App bundle',                z: '14MB',   c: '860MB',            r: 14 / 860,    x: '61×' },
  { icon: 'files',   label: 'Files in the bundle',       z: '5',      c: '17,035',           r: 5 / 17035,   x: '3,407×' },
  { icon: 'braces',  label: 'Shipped JavaScript',        z: '1.4MB',  c: '256MB',            r: 1.4 / 256,   x: '183×' },
  { icon: 'compass', label: 'Bundled runtime',           z: 'none — system WebKit', c: '259MB of Electron', r: 0, x: '' },
  { icon: 'grid',    label: 'Bundled extensions',        z: '0',      c: '116',              r: 0,           x: 'infinite' },
  { icon: 'watch',   label: 'Cold launch, to a window',  z: '0.38s',  c: '8.16s',            r: 0.38 / 8.16, x: '21×' },
  { icon: 'watch',   label: 'Warm launch, ready to use', z: '2.39s',  c: '6.59s',            r: 2.39 / 6.59, x: '2.8×' },
  { icon: 'chip',    label: 'Memory, one project',       z: '354MB',  c: '687MB',            r: 354 / 687,   x: '1.9×' },
  { icon: 'chip',    label: 'Memory, four projects',     z: '243MB',  c: '1,803MB',          r: 243 / 1803,  x: '7.4×' },
  { icon: 'gauge',   label: 'Idle CPU',                  z: '1.10%',  c: '2.66%',            r: 1.1 / 2.66,  x: '2.4×' },
];

const STATS = [
  { icon: 'box',   n: '14MB',  l: 'the whole app',           cv: '860MB',   r: 14 / 860,    x: '61×' },
  { icon: 'bolt',  n: '0.38s', l: 'cold launch to a window', cv: '8.16s',   r: 0.38 / 8.16, x: '21×' },
  { icon: 'chip',  n: '243MB', l: 'four projects open',      cv: '1,803MB', r: 243 / 1803,  x: '7.4×' },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>zero — a 14MB code editor</title>
    <meta
      name="description"
      content="A minimal macOS code editor built around running coding agents. 14MB, opens in 0.38s, 243MB with four projects open. press record, ramble, press stop."
    />
    <link rel="icon" href="/icon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon-32.png" type="image/png" sizes="32x32" />
    <link rel="apple-touch-icon" href="/icon.png" />
    <meta name="theme-color" content="#181818" />
    <meta property="og:title" content="zero — a 14MB code editor" />
    <meta
      property="og:description"
      content="A minimal macOS code editor built around running coding agents. press record, ramble, press stop."
    />
    <meta property="og:type" content="website" />
    <!-- Swap for the real domain's /icon.png once there is one; og wants absolute URLs. -->
    <meta property="og:image" content={`https://raw.githubusercontent.com/${REPO}/main/src-tauri/icons/128x128%402x.png`} />
    <meta name="twitter:card" content="summary" />
  </head>

  <body>
    <nav>
      <a class="wordmark" href="/">zero</a>
      <div class="nav-links">
        <a href="#points">why</a>
        <a href="#receipts">receipts</a>
        <a href="#install">install</a>
        <a href={GITHUB}>github ↗</a>
      </div>
    </nav>

    <header class="hero">
      <svg class="mark" viewBox="278 189 468 636" aria-hidden="true">
        <path
          fill="var(--accent)"
          fill-rule="evenodd"
          d="M 286,507 a 226,310 0 1,0 452,0 a 226,310 0 1,0 -452,0 Z M 354,507 a 158,266 0 1,1 316,0 a 158,266 0 1,1 -316,0 Z"
        ></path>
      </svg>
      <p class="kicker">your code editor is mostly decoration.</p>
      <h1>zero is what’s left.</h1>
      <p class="sub">
        A 14MB macOS code editor built around running coding agents: a terminal that takes the whole
        width, projects as tabs, git worktrees down the side, and an editor for when you actually need
        to read one of your files. That’s it — on purpose. It opens in 0.38s and has never once asked
        about your onboarding journey.
      </p>
      <div class="cta">
        <div class="brew">
          <code>brew install --cask zero-editor/zero/zero</code>
          <button class="copy" data-copy="brew install --cask zero-editor/zero/zero" aria-label="copy the brew command">copy</button>
        </div>
        <p class="cta-alt">
          or <a href={DMG}>download the dmg</a>
          <span class="release-extra"></span>
        </p>
      </div>
      <p class="fine">Apple Silicon · 17,593 lines · MIT · no network code in the app</p>
    </header>

    <section class="stats" aria-label="the numbers">
      {STATS.map((s) => (
        <div class="stat">
          <div class="stat-head">
            <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC[s.icon]} />
            <span class="x">{s.x}</span>
          </div>
          <div class="n">{s.n}</div>
          <div class="l">{s.l}</div>
          <div class="duo">
            <div class="drow"><span class="who">zero</span><span class="track"><i class="fill z" style={`width:${Math.max(s.r * 100, 0)}%`}></i></span><span class="val z">{s.n}</span></div>
            <div class="drow"><span class="who">cursor</span><span class="track"><i class="fill c" style="width:100%"></i></span><span class="val c">{s.cv}</span></div>
          </div>
        </div>
      ))}
    </section>
    <p class="stats-fine">
      Twenty-one times is not a typo — Cursor takes 21× longer to put a window on screen.
      Measured, not vibes: <a href={BENCHMARKS}>method and scripts →</a>
    </p>

    <section id="demo" class="demo">
      <div
        class="window"
        role="img"
        aria-label="zero’s window: projects across the titlebar with rings showing agent activity, file tabs below, a sidebar file tree, an editor, and a full-width terminal split in two"
      >
        <div class="titlebar">
          <span class="dot r"></span><span class="dot y"></span><span class="dot g"></span>
          <div class="tabs">
            <span class="tab active"><span class="ring working"></span>zero</span>
            <span class="tab"><span class="ring done"></span>api</span>
            <span class="tab">website</span>
          </div>
        </div>
        <div class="editor-tabs">
          <span class="etab active">smoothTermScroll.ts</span>
          <span class="etab mod">Titlebar.tsx</span>
          <span class="etab">ZERO.md</span>
        </div>
        <div class="body">
          <div class="side">
            <div class="ti"><span class="nm">▾ src</span></div>
            <div class="ti in1"><span class="nm">▾ components</span></div>
            <div class="ti in2 mod"><span class="nm">Titlebar.tsx</span><b>M</b></div>
            <div class="ti in2"><span class="nm">Sidebar.tsx</span></div>
            <div class="ti in1"><span class="nm">▾ lib</span></div>
            <div class="ti in2 mod"><span class="nm">smoothTermScroll.ts</span><b>M</b></div>
            <div class="ti in2 add"><span class="nm">memos.ts</span><b>A</b></div>
            <div class="ti in1"><span class="nm">App.css</span></div>
          </div>
          <div class="edit">
            <div class="cl"><span class="gut mod"></span>// The transform is never cleared to "" — always</div>
            <div class="cl"><span class="gut mod"></span>// translate3d(0,0,0): dropping it destroys the</div>
            <div class="cl"><span class="gut"></span>// composited layer, and the next gesture pays</div>
            <div class="cl"><span class="gut"></span>// 150ms to rasterise a new one.</div>
            <div class="cl"><span class="gut add"></span>screen.style.transform = keepAlive(offset);</div>
          </div>
        </div>
        <div class="term">
          <div class="pane">
            <div class="line"><span class="prompt">zero %</span> claude</div>
            <div class="line dim">✻ Thinking…</div>
            <div class="line dim">  ⎿ Reading smoothTermScroll.ts</div>
          </div>
          <div class="pane">
            <div class="line"><span class="prompt">zero %</span> <span class="cursor">▊</span></div>
          </div>
        </div>
      </div>
      <p class="caption">
        The whole interface: projects across the top, their files one row down, a tree at the side,
        and a terminal that takes the full width. The ring on a project is its agent — sweeping while
        it works, a closed circle when it’s waiting on you. No chrome; the buttons only appear when
        your pointer goes looking for them.
      </p>
    </section>

    <section id="points" class="points">
      <div class="point">
        <span class="idx">01</span>
        <div>
          <p class="head">every agent, at a glance.</p>
          <p class="plain">
            zero is built for running Claude in more than one project at once. Every project is a tab,
            and the ring on the tab is its agent — spinning while it works, a closed circle when it’s
            done and waiting on you. You glance, you know, you go back to what you were doing. No
            window juggling, no “which terminal beeped”.
          </p>
          <p class="tech">
            It watches each shell’s process tree once a second for a live claude, then reads how long
            the pty has been silent. No integration, no cooperation from the agent required.
          </p>
        </div>
      </div>
      <div class="point">
        <span class="idx">02</span>
        <div>
          <p class="head"><span class="rec" aria-hidden="true"></span>press record, ramble, press stop.</p>
          <p class="plain">
            Talk at your project instead of typing at it. Your Mac writes down what you said, claude
            tidies it into the note you meant, and recording on top of a memo folds the new thoughts
            in. Four minutes of thinking out loud, one document you’d actually show someone.
          </p>
          <p class="tech">
            Transcription is macOS 26’s own, on this Mac. One claude -p pass makes the concise
            version. It’s files all the way down — audio, transcripts, document, all in .zero/memos/ —
            and a ZERO.md of proper nouns keeps “Anthropic” from coming back as “entropic”.
          </p>
        </div>
      </div>
      <div class="point">
        <span class="idx">03</span>
        <div>
          <p class="head">it’s barely even there.</p>
          <p class="plain">
            14MB on disk. A window in 0.38s. A quarter of a gigabyte with four projects open. Your RAM
            goes to the agents doing the work, not to the app they run in — an editor should be a
            tool, not a tenant.
          </p>
          <p class="tech">
            Numbers below, method in BENCHMARKS.md, scripts in bench/ if you’d like to run them on
            your own machine.
          </p>
        </div>
      </div>
    </section>

    <section id="receipts" class="receipts">
      <h2>receipts</h2>
      <p class="method">
        Same machine, same project, both editors, three launches each, median reported.
        M3&nbsp;Max · 36GB · macOS&nbsp;26.5.1 · Cursor&nbsp;3.15.6. We didn’t have to cherry-pick.
      </p>
      <div class="sheet">
        {RECEIPTS.map((row) => (
          <div class="rrow">
            <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC[row.icon]} />
            <div class="rlabel">{row.label}</div>
            <div class="duo">
              <div class="drow">
                <span class="who">zero</span>
                <span class="track">{row.r > 0 && <i class="fill z" style={`width:${row.r * 100}%`}></i>}</span>
                <span class="val z">{row.z}</span>
              </div>
              <div class="drow">
                <span class="who">cursor</span>
                <span class="track"><i class="fill c" style="width:100%"></i></span>
                <span class="val c">{row.c}</span>
              </div>
            </div>
            <div class="rx">{row.x}</div>
          </div>
        ))}
      </div>
      <p class="note">
        It gets wider the more projects you open: each one costs Cursor about 360MB and three
        processes, linearly — a window per folder. zero opens a tab, and its process count never
        moves off five. Also: 125fps terminal scrolling on a ProMotion display, against the stock
        WebView’s 59.
      </p>
      <aside class="caveat">
        The caveat that matters: Cursor does enormously more — language servers, extensions,
        debugging, an actual AI product. Most of the gap above is that difference priced in bytes and
        milliseconds, not better engineering. We just noticed which part you were using.
        <a href={BENCHMARKS}>Method, caveats, and the scripts to reproduce it →</a>
      </aside>
    </section>

    <section class="features">
      <h2>what’s in it</h2>
      <div class="grid">
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.tabs} />projects are tabs</h3>
          <p>
            Each keeps its own terminals, sidebar and open files. Switching is a compositor swap —
            everything stays laid out and painted, nothing re-fits when you come back.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.term} />the terminal is the point</h3>
          <p>
            Full window width, splits in any direction, draggable dividers. No title, no bar, no rule —
            buttons appear only when the pointer goes looking for them.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.branch} />worktrees, not branches</h3>
          <p>
            The sidebar lists every worktree with its changes, staging and commit box. Which is the
            shape of the work when several agents are going at once.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.bars} />bars for what you changed</h3>
          <p>
            Green for added, blue for edited, a red wedge where lines were deleted — in the gutter,
            again beside the scrollbar, and up the file tree, so an edit three folders deep is visible
            without opening anything.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.search} />⌘P, and search in Rust</h3>
          <p>
            Fuzzy go-to-file over what git will admit to, so <code>node_modules</code> never appears.
            Project search runs in-process: 53ms warm on a 6,777-file repo, where ripgrep took 83.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.braces} />143 languages</h3>
          <p>
            The seven zero is written in are compiled in; the rest load the first time you open one, so
            a project with no Ruby in it never pays for the Ruby mode.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.gauge} />120fps in a WebView</h3>
          <p>
            WKWebView clamps rendering to 60fps. Safari turns the clamp off through a private flag, so
            zero flips the same one at startup. This rules out the Mac App Store forever. We’re at
            peace.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.cursor} />⌘-click anything</h3>
          <p>
            A name in the editor opens where it’s defined, through path aliases. A path in the terminal
            opens beside it — <code>file.ts:42</code> lands on the line — and URLs open in your browser.
          </p>
        </div>
        <div class="card">
          <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.restore} />it opens how you left it</h3>
          <p>
            Projects, splits, sidebar, tabs — restored. The shells are new, because they die with the
            app; <code>claude --continue</code> picks the conversation back up, which is the part you
            wanted.
          </p>
        </div>
      </div>
      <div class="card wide">
        <h3><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" set:html={IC.slash} />what’s not in it</h3>
        <p>
          An extension marketplace. A sign-in. A welcome tour. A release-notes popup. A settings sync
          service. A chat panel you can’t close — claude lives in the terminal, where you put it.
          Network code of any kind: the only thing that ever leaves this Mac is what you were already
          typing into <code>claude</code>. The absence is the feature.
        </p>
      </div>
    </section>

    <section id="install" class="install">
      <h2>install</h2>
      <div class="brew">
        <code>brew install --cask zero-editor/zero/zero</code>
        <button class="copy" data-copy="brew install --cask zero-editor/zero/zero" aria-label="copy the brew command">copy</button>
      </div>
      <p class="alt">
        Or <a href={DMG}>download the dmg</a><span class="release-extra"></span>
        and drag zero to Applications. Apple Silicon, macOS, <code>git</code> — nothing else.
      </p>
      <aside class="unsigned">
        <h3>about the “damaged” message</h3>
        <p>
          The app isn’t signed — a proper signature needs an Apple Developer account, and $99 a year
          buys a lot of nothing. So macOS quarantines the download and announces that zero “is damaged
          and can’t be opened”. It isn’t damaged; that’s the message for <em>unsigned</em>. Homebrew
          clears the flag itself, which is the whole reason to prefer it. After a manual download,
          clear it yourself, once:
        </p>
        <code class="block">xattr -dr com.apple.quarantine /Applications/zero.app</code>
      </aside>
      <p class="fine">
        Or <a href={`${GITHUB}#or-build-it`}>build it from source</a> in about a minute. The whole
        thing is 17,593 lines — you could read it in an afternoon.
      </p>
    </section>

    <footer>
      <div class="foot-row">
        <span class="wordmark">zero</span>
        <div class="foot-links">
          <a href={GITHUB}>github</a>
          <a href={BENCHMARKS}>benchmarks</a>
          <a href={`${GITHUB}/releases`}>releases</a>
          <a href={`${GITHUB}/blob/main/LICENSE`}>mit</a>
        </div>
      </div>
      <p>Made by Lucian and Vid — from Slovenia, with love and a small <code>phys_footprint</code>.</p>
      <p>It’s zero, not Zero. Even at the start of a sentence. Especially there.</p>
      <p>
        This page is one HTML file with no framework in it — about 8KB over the wire, a third of what
        the average one of Cursor’s 12,021 JavaScript files weighs.
      </p>
    </footer>

    <script is:inline>
      // The site's entire JavaScript: ask GitHub what the newest release is,
      // and make the copy buttons copy. If any of it fails, the page has
      // already said everything it needed to.
      (async () => {
        try {
          const r = await fetch('https://api.github.com/repos/zero-editor/zero/releases/latest');
          if (!r.ok) return;
          const rel = await r.json();
          const v = (rel.tag_name || '').replace(/^v/, '');
          const date = rel.published_at
            ? new Date(rel.published_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })
            : '';
          const dmg = (rel.assets || []).find((a) => a.name === 'zero_aarch64.dmg');
          const size = dmg ? `${(dmg.size / 1048576).toFixed(1)}MB` : '';
          const bits = [v && `v${v}`, date, size].filter(Boolean).join(' · ');
          if (!bits) return;
          for (const el of document.querySelectorAll('.release-extra')) {
            el.textContent = ` — ${bits}`;
          }
        } catch {}
      })();

      for (const btn of document.querySelectorAll('.copy')) {
        btn.addEventListener('click', async () => {
          try {
            await navigator.clipboard.writeText(btn.dataset.copy);
            btn.textContent = 'copied';
            setTimeout(() => (btn.textContent = 'copy'), 1600);
          } catch {}
        });
      }
    </script>
  </body>
</html>

<style is:global>
  /* Tokens lifted from the app's App.css. The accent isn't a hue: everything
     that wants to stand out here does it by being brighter, which is the only
     axis this theme has. */
  :root {
    --bg: #1f1f1f;
    --bg-panel: #181818;
    --bg-raised: #2a2d2e;
    --border: #2b2b2b;
    --text: #cccccc;
    --text-dim: #9d9d9d;
    --text-faint: #656565;
    --accent: #e6e6e6;
    --green: #73c991;
    --red: #f14c4c;
    --yellow: #e2c08d;
    --blue: #4f8bd6;
    --bar-c: #4a4a4a;
    --font-ui: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
    --font-mono: ui-monospace, 'SF Mono', Menlo, Monaco, 'Cascadia Mono', monospace;
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    background: var(--bg);
    scroll-behavior: smooth;
  }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-ui);
    font-size: 15px;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  ::selection {
    background: var(--accent);
    color: var(--bg);
  }

  a {
    color: var(--text-dim);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: var(--border);
    transition: color 120ms;
  }

  a:hover {
    color: var(--accent);
  }

  code {
    font-family: var(--font-mono);
    font-size: 0.92em;
  }

  h2 {
    font-family: var(--font-mono);
    font-size: 14px;
    font-weight: 400;
    letter-spacing: 0.25em;
    color: var(--text-faint);
    margin-bottom: 28px;
  }

  .ic {
    width: 17px;
    height: 17px;
    flex: none;
    color: var(--text-faint);
  }

  section,
  header.hero {
    max-width: 960px;
    margin: 0 auto;
    padding: 72px 24px;
  }

  /* ===== nav ===== */

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px 24px;
    border-bottom: 1px solid var(--border);
    background: var(--bg-panel);
  }

  .wordmark {
    font-family: var(--font-mono);
    font-size: 15px;
    letter-spacing: 0.35em;
    margin-right: -0.35em;
    color: var(--text-dim);
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: 22px;
  }

  .nav-links a {
    font-family: var(--font-mono);
    font-size: 12px;
    text-decoration: none;
  }

  /* ===== hero ===== */

  .hero {
    text-align: center;
    padding-top: 84px;
  }

  .mark {
    height: 164px;
    margin-bottom: 40px;
  }

  .kicker {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text-faint);
    margin-bottom: 14px;
  }

  h1 {
    font-size: clamp(34px, 6vw, 58px);
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--accent);
    line-height: 1.1;
    margin-bottom: 22px;
  }

  .sub {
    max-width: 660px;
    margin: 0 auto 36px;
    color: var(--text-dim);
  }

  .cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
  }

  .brew {
    display: inline-flex;
    align-items: center;
    gap: 18px;
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 13px 18px;
  }

  .brew code {
    font-size: 13.5px;
    color: var(--accent);
    white-space: nowrap;
  }

  .copy {
    background: none;
    border: 1px solid var(--border);
    border-radius: 5px;
    color: var(--text-faint);
    font-family: var(--font-mono);
    font-size: 11px;
    padding: 3px 8px;
    cursor: pointer;
    transition: color 120ms, border-color 120ms;
  }

  .copy:hover {
    color: var(--accent);
    border-color: var(--text-faint);
  }

  .cta-alt,
  .install .alt {
    color: var(--text-dim);
    font-size: 14px;
  }

  .release-extra {
    font-family: var(--font-mono);
    font-size: 12.5px;
    color: var(--text-faint);
  }

  .fine {
    margin-top: 26px;
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-faint);
  }

  /* ===== stats ===== */

  .stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    padding-top: 0;
    padding-bottom: 14px;
  }

  .stat {
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-panel);
    padding: 22px 24px 20px;
  }

  .stat-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
  }

  .x {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--green);
  }

  .stat .n {
    font-family: var(--font-mono);
    font-size: clamp(30px, 4vw, 40px);
    color: var(--accent);
    line-height: 1.1;
  }

  .stat .l {
    color: var(--text-dim);
    font-size: 13.5px;
    margin-top: 5px;
    margin-bottom: 16px;
  }

  /* the little zero-vs-cursor bar pair, shared by tiles and receipts */

  .duo {
    display: flex;
    flex-direction: column;
    gap: 7px;
    min-width: 0;
  }

  .drow {
    display: grid;
    grid-template-columns: 44px 1fr auto;
    align-items: center;
    gap: 10px;
  }

  .who {
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--text-faint);
  }

  .track {
    height: 4px;
    border-radius: 2px;
    overflow: hidden;
    background: transparent;
  }

  .fill {
    display: block;
    height: 100%;
    border-radius: 2px;
  }

  .fill.z {
    background: var(--accent);
    min-width: 2px;
  }

  .fill.c {
    background: var(--bar-c);
  }

  .val {
    font-family: var(--font-mono);
    font-size: 11.5px;
    white-space: nowrap;
  }

  .val.z {
    color: var(--accent);
  }

  .val.c {
    color: var(--text-faint);
  }

  .stats-fine {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 24px 24px;
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-faint);
    text-align: center;
  }

  /* ===== demo window ===== */

  .demo {
    padding-top: 34px;
    text-align: center;
  }

  .window {
    max-width: 840px;
    margin: 0 auto;
    text-align: left;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 10px;
    box-shadow: 0 24px 70px rgba(0, 0, 0, 0.45);
    overflow: hidden;
  }

  .titlebar {
    display: flex;
    align-items: center;
    gap: 7px;
    background: var(--bg-panel);
    border-bottom: 1px solid var(--border);
    padding: 10px 12px;
  }

  .dot {
    width: 11px;
    height: 11px;
    border-radius: 50%;
    flex: none;
  }

  .dot.r { background: #f1584c; }
  .dot.y { background: #e2c08d; }
  .dot.g { background: #73c991; }

  .tabs {
    display: flex;
    gap: 4px;
    margin-left: 14px;
    overflow: hidden;
  }

  .tab {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--text-faint);
    padding: 4px 11px;
    border-radius: 6px;
    white-space: nowrap;
  }

  .tab.active {
    background: var(--bg);
    color: var(--text);
  }

  /* the ring, to the app's own spec: a white arc with a comet tail while an
     agent works, over a faint full track; a closed white circle once it's
     quiet and waiting on you */

  .ring {
    position: relative;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    flex: none;
  }

  .ring.working {
    background: conic-gradient(
      from 0deg,
      transparent 0deg 250deg,
      rgba(255, 255, 255, 0.5) 330deg,
      #ffffff 358deg,
      transparent 360deg
    );
    -webkit-mask: radial-gradient(farthest-side, transparent calc(100% - 1.4px), #000 calc(100% - 1.4px));
    mask: radial-gradient(farthest-side, transparent calc(100% - 1.4px), #000 calc(100% - 1.4px));
    filter: drop-shadow(0 0 2px rgba(255, 255, 255, 0.5));
    animation: sweep 1.5s linear infinite;
  }

  .ring.working::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 1.4px solid rgba(255, 255, 255, 0.07);
  }

  .ring.done {
    border: 1.4px solid #ffffff;
    box-shadow: 0 0 4px 0 rgba(255, 255, 255, 0.4);
  }

  @keyframes sweep {
    to { transform: rotate(360deg); }
  }

  .editor-tabs {
    display: flex;
    background: var(--bg-panel);
    border-bottom: 1px solid var(--border);
    padding: 0 12px;
    gap: 2px;
  }

  .etab {
    font-family: var(--font-mono);
    font-size: 11.5px;
    color: var(--text-faint);
    padding: 7px 12px;
    white-space: nowrap;
  }

  .etab.active {
    color: var(--text);
    background: var(--bg);
    border-radius: 6px 6px 0 0;
  }

  .etab.mod {
    color: var(--yellow);
  }

  .body {
    display: grid;
    grid-template-columns: 172px 1fr;
  }

  .side {
    border-right: 1px solid var(--border);
    background: var(--bg-panel);
    padding: 10px 0 12px;
    font-family: var(--font-mono);
    font-size: 11px;
    line-height: 1.9;
    color: var(--text-dim);
  }

  .ti {
    display: flex;
    align-items: baseline;
    gap: 8px;
    padding: 0 12px;
  }

  .ti .nm {
    flex: 1 1 auto;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .ti.in1 { padding-left: 24px; }
  .ti.in2 { padding-left: 36px; }

  .ti b {
    flex: none;
    font-weight: 400;
  }

  .ti.mod, .ti.mod b { color: var(--yellow); }
  .ti.add, .ti.add b { color: var(--green); }

  .edit {
    padding: 12px 0 14px;
    font-family: var(--font-mono);
    font-size: 11.5px;
    line-height: 1.9;
    color: var(--text-faint);
    overflow: hidden;
  }

  .cl {
    display: flex;
    align-items: stretch;
    white-space: nowrap;
  }

  /* the change gutter: a bar against every uncommitted line */
  .gut {
    width: 3px;
    margin: 2px 12px 2px 8px;
    border-radius: 1px;
    flex: none;
  }

  .gut.mod { background: var(--blue); }
  .gut.add { background: var(--green); }

  .term {
    display: grid;
    grid-template-columns: 1.7fr 1fr;
    border-top: 1px solid var(--border);
    font-family: var(--font-mono);
    font-size: 12.5px;
    line-height: 1.75;
    min-height: 108px;
  }

  .pane {
    padding: 14px 16px 18px;
  }

  .pane + .pane {
    border-left: 1px solid var(--border);
  }

  .prompt {
    color: var(--text-dim);
  }

  .line.dim {
    color: var(--text-faint);
  }

  .cursor {
    animation: blink 1.2s step-end infinite;
    color: var(--text-dim);
  }

  @keyframes blink {
    50% { opacity: 0; }
  }

  .caption {
    max-width: 640px;
    margin: 28px auto 0;
    color: var(--text-dim);
    font-size: 14px;
  }

  /* ===== the three points ===== */

  .points {
    display: flex;
    flex-direction: column;
    gap: 64px;
  }

  .point {
    display: grid;
    grid-template-columns: 72px 1fr;
    gap: 8px;
  }

  .idx {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--text-faint);
    padding-top: 9px;
  }

  .head {
    font-family: var(--font-mono);
    font-size: clamp(21px, 3.4vw, 32px);
    color: var(--accent);
    line-height: 1.3;
    margin-bottom: 14px;
    letter-spacing: -0.01em;
  }

  .rec {
    display: inline-block;
    width: 0.42em;
    height: 0.42em;
    border-radius: 50%;
    background: var(--red);
    margin-right: 0.5em;
    vertical-align: 0.06em;
    animation: pulse 1.6s ease-in-out infinite;
  }

  @keyframes pulse {
    50% { opacity: 0.25; }
  }

  .plain {
    max-width: 620px;
    color: var(--text-dim);
  }

  .tech {
    max-width: 620px;
    margin-top: 12px;
    font-family: var(--font-mono);
    font-size: 12px;
    line-height: 1.7;
    color: var(--text-faint);
  }

  /* ===== receipts ===== */

  .method {
    color: var(--text-faint);
    font-size: 13.5px;
    margin-bottom: 24px;
    max-width: 720px;
  }

  .sheet {
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-panel);
    overflow: hidden;
  }

  .rrow {
    display: grid;
    grid-template-columns: 17px 200px 1fr 74px;
    gap: 18px;
    align-items: center;
    padding: 13px 20px;
    border-bottom: 1px solid var(--border);
  }

  .rrow:last-child {
    border-bottom: none;
  }

  .rlabel {
    font-size: 13.5px;
    color: var(--text-dim);
  }

  .rx {
    font-family: var(--font-mono);
    font-size: 12.5px;
    color: var(--green);
    text-align: right;
    white-space: nowrap;
  }

  .note {
    color: var(--text-dim);
    font-size: 14px;
    margin-top: 22px;
    max-width: 720px;
  }

  .caveat {
    margin-top: 26px;
    padding: 18px 20px;
    border-left: 2px solid var(--border);
    color: var(--text-faint);
    font-size: 13.5px;
    max-width: 720px;
  }

  .caveat a {
    display: inline-block;
    margin-top: 8px;
  }

  /* ===== features ===== */

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
    gap: 14px;
  }

  .card {
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-panel);
    padding: 20px 22px;
  }

  .card h3 {
    display: flex;
    align-items: center;
    gap: 9px;
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 400;
    color: var(--accent);
    margin-bottom: 10px;
  }

  .card h3 .ic {
    width: 16px;
    height: 16px;
  }

  .card p {
    color: var(--text-dim);
    font-size: 13.5px;
  }

  .card.wide {
    margin-top: 14px;
  }

  /* ===== install ===== */

  .install .brew {
    margin-bottom: 16px;
  }

  .unsigned {
    margin-top: 30px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--bg-panel);
    padding: 22px 24px;
    max-width: 720px;
  }

  .unsigned h3 {
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 400;
    color: var(--yellow);
    margin-bottom: 10px;
  }

  .unsigned p {
    color: var(--text-dim);
    font-size: 13.5px;
  }

  code.block {
    display: block;
    margin-top: 14px;
    padding: 11px 14px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 7px;
    font-size: 13px;
    color: var(--accent);
    user-select: all;
    -webkit-user-select: all;
    overflow-x: auto;
    white-space: nowrap;
  }

  .install .fine {
    margin-top: 24px;
  }

  /* ===== footer ===== */

  footer {
    border-top: 1px solid var(--border);
    background: var(--bg-panel);
    padding: 44px 24px 56px;
  }

  footer > * {
    max-width: 960px;
    margin-left: auto;
    margin-right: auto;
  }

  .foot-row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 26px;
  }

  .foot-links {
    display: flex;
    gap: 20px;
  }

  .foot-links a {
    font-family: var(--font-mono);
    font-size: 12px;
    text-decoration: none;
  }

  footer p {
    color: var(--text-faint);
    font-size: 13px;
    margin-top: 8px;
  }

  /* ===== small screens ===== */

  @media (max-width: 720px) {
    section,
    header.hero {
      padding: 52px 18px;
    }

    .hero {
      padding-top: 56px;
    }

    .mark {
      height: 118px;
      margin-bottom: 30px;
    }

    .stats {
      grid-template-columns: 1fr;
    }

    .brew {
      max-width: 100%;
      overflow-x: auto;
    }

    .body {
      grid-template-columns: 1fr;
    }

    .side {
      display: none;
    }

    .editor-tabs {
      overflow-x: auto;
      scrollbar-width: none;
    }

    /* splits go in any direction; on a phone, down */
    .term {
      grid-template-columns: 1fr;
      min-height: 0;
    }

    .pane + .pane {
      border-left: none;
      border-top: 1px solid var(--border);
    }

    .point {
      grid-template-columns: 1fr;
      gap: 2px;
    }

    .rrow {
      grid-template-columns: 17px 1fr 74px;
      grid-template-rows: auto auto;
      row-gap: 10px;
    }

    .rrow .duo {
      grid-column: 1 / -1;
    }

    .foot-row {
      flex-direction: column;
      gap: 14px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ring.working,
    .cursor,
    .rec {
      animation: none;
    }
  }
</style>
